# Yufeng Liu Portfolio Website

Static portfolio site for public deployment with GitHub Pages.

## Local preview

From this repository root, run:

```powershell
python -m http.server 4179
```

Then open:

`http://localhost:4179/`
